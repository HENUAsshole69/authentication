spring:
  datasource:
    url: jdbc:mysql://localhost:3306/auth
    password: lujc781chendk46
    username: root
  jpa:
    hibernate:
      ddl-auto: update
HMAC256:
  secret: secret
server:
  servlet:
    context-path: /api
